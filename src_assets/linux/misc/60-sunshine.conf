# Sunshine needs uhid for DS5 emulation
uhid
# Zenith display autopilot: evdi backs the universal virtual-display fallback.
# Loading it at boot means no runtime privileges are needed to create a VDD.
evdi
